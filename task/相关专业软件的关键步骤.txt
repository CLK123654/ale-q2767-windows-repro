1.读取现行Namespace和Pod清单，按namespace与name确定发布对象。
2.为cache-writer和test-runner补齐Pod与容器安全上下文，并把test-runner的hostPath改为emptyDir。
3.读取stages.csv和namespace-policy.csv，为current、baseline和restricted准备Namespace标签与Kustomize入口。
4.分别执行kubectl kustomize，保存渲染清单并核对对象身份。
5.从渲染结果整理Namespace标签、Pod变化和保留项，再按窗口计划编制发布安排。
6.核对发布顺序、影响范围、等待预算、观察时长和回退责任，完成交接说明。
